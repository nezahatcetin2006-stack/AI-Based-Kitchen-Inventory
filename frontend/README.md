# Frontend (Next.js)

## Geliştirme
1. `frontend/.env.local.example` dosyasını kopyalayın: `.env.local`
2. Backend’in ayağa kalktığından emin olun.
3. Çalıştırın:
   - `npm install`
   - `npm run dev`

Varsayılan olarak UI, `NEXT_PUBLIC_API_BASE_URL` üzerinden backend’e istek atar.

